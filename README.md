## Requirements
- Python 3.x
- No external libraries needed

- A Python CLI app for user registration, login, and account deletion.
User data is stored locally in a `users.json` file.

##Features
- Register with username/password validation
- Login with credential checking
- Delete account with confirmation
- Data stored inJSON file

